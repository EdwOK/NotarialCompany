<UserControl 
    x:Class="NotarialCompany.Pages.LoginPage.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
    xmlns:con="http://schemas.kent.boogaart.com/converters"
    DataContext="{Binding LoginViewModel, Source={StaticResource Locator}}"
    mc:Ignorable="d" 
    d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" MinWidth="300">
        <xctk:WatermarkTextBox Style="{StaticResource TextBoxValidationStyle}" Name="Login" Watermark="Login" Margin="0,0,0,15" Text="{Binding Login, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}" />
        <xctk:WatermarkTextBox Style="{StaticResource TextBoxValidationStyle}" Name="Password" Watermark="Password" Margin="0,0,0,5" Text="{Binding Password, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}" />
        <Label Content="{Binding LoginErrorMessage, Mode=TwoWay}" Margin="0,0,0,5" Foreground="Red" Visibility="{Binding HasLoginError, Converter={con:BooleanToVisibilityConverter IsReversed=True}}" />
        <Button Content="Login" Command="{Binding LoginCommand}" IsDefault="True" Style="{StaticResource GreenButton}" IsEnabled="{Binding Error, Converter={StaticResource NullToTrueConverter}}"/>
    </StackPanel>
</UserControl>