<UserControl 
    x:Class="NotarialCompany.Pages.LoginPage.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
    xmlns:con="http://schemas.kent.boogaart.com/converters"
    DataContext="{Binding LoginViewModel, Source={StaticResource Locator}}"
    mc:Ignorable="d" 
    d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" MinWidth="250">
        <xctk:WatermarkTextBox Style="{StaticResource ValidateTextBox}" Name="Login" Watermark="Login" Margin="0,0,0,15" Text="{Binding Login, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}" />
        <xctk:WatermarkTextBox Style="{StaticResource ValidateTextBox}" Name="Password" Watermark="Password" Margin="0,0,0,5" Text="{Binding Password, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}" />
        <Label Content="{Binding ErrorMessage, Mode=TwoWay}" Margin="0,0,0,5" Foreground="Red" Visibility="{Binding HasError, Converter={con:BooleanToVisibilityConverter IsReversed=True}}" />
        <Button Content="Login" Command="{Binding LoginCommand}" IsDefault="True">
            <Button.Style>
                <Style TargetType="{x:Type Button}" BasedOn="{StaticResource GreenButton}">
                    <Setter Property="IsEnabled" Value="False" />
                    <Style.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding ElementName=Login, Path=(Validation.HasError)}" Value="false" />
                                <Condition Binding="{Binding ElementName=Password, Path=(Validation.HasError)}" Value="false" />
                            </MultiDataTrigger.Conditions>
                            <Setter Property="IsEnabled" Value="True" />
                        </MultiDataTrigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>
    </StackPanel>
</UserControl>