<metroControls:MetroContentControl 
    x:Class="NotarialCompany.Pages.LoginPage.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:con="http://schemas.kent.boogaart.com/converters"
    xmlns:metroControls="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
    xmlns:controls="clr-namespace:NotarialCompany.Controls"
    DataContext="{Binding LoginViewModel, Source={StaticResource Locator}}"
    mc:Ignorable="d" 
    d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" MinWidth="300">
        <TextBox Name="Login" metroControls:TextBoxHelper.Watermark="Login" Margin="0,0,0,15" Text="{Binding Login, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}" />
        <PasswordBox Name="Password" metroControls:TextBoxHelper.Watermark="Password" Margin="0,0,0,5">
            <i:Interaction.Behaviors>
                <controls:PasswordBehavior Password="{Binding Password, Mode=TwoWay, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}" />
            </i:Interaction.Behaviors>
        </PasswordBox>
        <Label Content="{Binding LoginErrorMessage, Mode=TwoWay}" Margin="0,0,0,5" Foreground="Red" Visibility="{Binding HasLoginError, Converter={con:BooleanToVisibilityConverter IsReversed=True}}" />
        <Button Content="Login" Command="{Binding LoginCommand}" IsDefault="True" IsEnabled="{Binding Error, Converter={StaticResource NullToTrueConverter}}"/>
    </StackPanel>
</metroControls:MetroContentControl>